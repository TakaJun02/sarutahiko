# UI スタイル刷新仕様（FR-12）— デザインリード: Codex / GPT-5.6Sol

- 版: v0.7（2026-07-14, Fable 起草 — **FR-17 追補 §13**: FR-16 の 2 演出の知覚強度引き上げ。
  待機中アンビエントを「雲が流れる」ドリフトへ再設計し、composer フォーカス枠の色サイクルを高速化）
- v0.6（2026-07-14, Fable 起草・検収済 — **FR-16 追補 §12**: 回答待機中のアンビエント・アニメーションと、
  composer フォーカス枠の細線化＋アイコン色スロー・サイクル。実機検収合格は merge 86d15e8。
  同日、利用者フィードバックで知覚強度不足が判明し FR-17 §13 で強化）
- v0.5（2026-07-14, Fable 起草・検収済 — **FR-15 追補 §11**: 空状態カウントダウンチップの撤去・
  チャットヘッダーの整理（アイコン/サブラベル削除）・サジェストチップの「押せる」刷新＋小型化。
  実機検収合格は merge c9c1d6e）
- v0.4（2026-07-14, Fable 起草 — **FR-14 追補 §10**: 利用者提供の Gemini 実スクショ（`referenceUI/`）
  準拠でアンビエント背景を再仕様化。§9-1 の clay/slate/moss 雲を置き換える。実測色の合わせ込み目標と
  ピクセル機械照合の検収を定義）
- v0.3（2026-07-14, Fable 起草・検収済 — FR-13 追補 §9。実機検収合格は commit 688b411）
- v0.2（2026-07-14, Fable 追記 — **検収完了・合格**。評価エージェント採点はラウンド 1 で
  **94/100**（合格基準 93 以上を一発達成。内訳: 第一印象9/配色10/タイポ9/余白9/一貫性9/マイクロ10/
  モバイル9/PC9/世界観10/細部10）。不可侵制約 §2 は全項目維持を実機確認（LoadingSpinnerV5 差分ゼロ・
  実況テキスト 2.5s 遷移・タイプライター実測 150ms/字・外部リクエスト 0 件・reduced-motion 完全対応）。
  R1 の軽微減点 4 件（挨拶見出しの孤立行・CTA 色規則・768px 余白・composer focus 視認性）は任意対応として
  Sol に差し戻し、Round 2 で全件対応済み（テスト 11/11・ビルド成功・Fable 実機再確認済み）。
  Sol 設計ノートは §8）
- v0.1（2026-07-14, Fable 起草）
- 対象: ログイン画面・チャット画面（サイドバー・ドロワー・ダイアログ・空状態・入力バー含む）
- 検収方式: FR-9 と同じ評価エージェント採点ループ（§7。合格基準は引き上げ）

## 0. 発端（利用者指示 2026-07-14）

> UIをブラッシュアップしてください。単に色を多用して派手にするのではなく、かっこいい見た目にしてください。
> いかにもAIが作ったUIという雰囲気がします。センスの感じるカッコ良いアプリUIを作成してください。
> 個人的にはGeminiアプリのUIがスタイリッシュでセンスの感じるかっこいいUIだと感じます。
> 静止画の見た目だけでなく、アニメーションや演出もこだわってください。
> ログイン画面と会話画面の両方ブラッシュアップしてください。
> ※このシステムはPCでもスマホでも使用します。両方で最適化された見た目にしてください。
> デザインについては、CodexでGPT-5.6Solがメインで動いてください。

### 役割（本 FR に限る特例）

| 役割 | 担当 |
|---|---|
| デザインの最終決定（§2 の制約内） | **Codex / GPT-5.6Sol** |
| 制約定義・アセット支給・検収 | Fable |

- 本 FR では **Sol がデザイン決定権を持つ**。§4 の「推奨方向」は Fable の分析に基づく参考案であり、
  Sol がより良い解を持つならそちらを採ってよい（採らなかった推奨・破った推奨は、完了報告に設計意図を 1 行ずつ記す）。
- ただし §2（不可侵の制約）と §0 の利用者の言葉（派手にしない・色を絞る・Gemini 的な抑制・PC/スマホ両対応・
  アニメーションもこだわる）だけは何があっても守る。

## 1. 現状の課題（Fable の分析、2026-07-14 実機スクリーンショットより）

1. **ブランド不在**: ログイン画面にロゴ・アプリ名・大学名が一切ない。何のアプリか画面から分からない。
2. **PC ログインの間延び**: 1440px で白面が画面の 2/3 を占めて空虚。黒シートは全幅に伸び、max-w-md の
   フォームだけが黒い海に浮く。モバイル（390px）では良く見える構図が、PC で設計されていない。
3. **アクセントの散乱**: 3 色グラデ（#FF8A65→#FFEB3B→#69F0AE）がヘッダー下ヘアライン・カウントダウン
   チップ枠・アクティブスレッドバー・アバター・送信ボタン・入力欄のミント発光…と多所に出現。
   「一本の糸」の意図（FR-9）が実装では「虹の散乱」になっており、利用者の言う「色を多用して派手」の主因。
4. **ユーザー吹き出しがほぼ純白**: ダーク画面で眩しく浮く。Gemini のダークテーマはユーザー発言も
   ソフトな面色（明度を落としたグレー）＋白文字で、画面全体の明度が静か。
5. **タイポグラフィの個性ゼロ**: 全要素が Noto Sans の似たウェイト・サイズ階層。数字（カウントダウン・
   時刻表・「2026」）にも表情がない。
6. **演出の不足**: ローディングリング（確定仕様）以外に演出がほぼない。画面遷移はぶつ切り、空状態・
   メッセージ出現・ログイン成功時に何も起きない。
7. 総評: 破綻はないが「量産型ダーク AI チャット」テンプレの域を出ない。これが「いかにも AI が作った UI」
   の正体と分析する。

## 2. 不可侵の制約（MUST — これだけは破らない）

1. **LoadingSpinnerV5（Aurora Ring）**: 動作・タイミング・ステップ別テーマ色・morph（pending→settled）は
   `docs/UI_LOADING_ANIMATION.md` の確定仕様。リング周囲の「見せ方」（余白・整列・背景との調和）は調整可、
   リング自体（SVG 構造・アニメーション値・STEP_THEMES・実況テキストの shimmer）は不可侵。
   Q-013 の `type="transition"` 修正も維持する。
2. **ログインの骨格**: 「ライトエリア（タイプライター見出し＋点滅カーソル）＋下部シート」の 2 層構造、
   タイプライターの 8 フレーズと打鍵タイミング（150/75/2000/500ms）、パスワードレス（ニックネームのみ）は
   変更不可。それ以外 — シートの色・角丸・内部レイアウト、ライトエリアへの要素追加（ブランド行・背景表情）、
   両エリアの面積比・PC での構図 — は Sol の裁量（`docs/UI_LOGIN.md` v0.4 で緩和済み）。
3. **チャットの中央カラム原則**（`docs/UI_CHAT.md` §1: AI 回答=中央ドキュメント型 / ユーザー発言=右寄せ
   吹き出し）とサイドバーの機能要件（§4）。※ユーザー吹き出しの「白」は v0.3 で解除済み — 色は Sol の裁量。
4. **表現層のみ変更**: SSE スキーマ・stores（auth/chat）の公開 API・router のルート定義・backend は
   変更しない。`chat.spec.js` が壊れる store 変更は不可。
5. **アクセシビリティ**: タップターゲット 44×44px 以上、コントラスト本文 4.5:1・補助 3:1 以上、
   全インタラクティブ要素に focus-visible リング、`prefers-reduced-motion` で装飾モーション全停止（機能は保つ）。
6. **オフライン堅牢性**: 実行時に外部ネットワークへ出る資産（CDN フォント・外部画像・外部 CSS/JS）を
   追加しない。全アセットはリポジトリ同梱でビルドに含める（会場ネットワークが不安定でも崩れないこと）。
7. **新規 npm 依存の追加は不可**（アニメーションライブラリ含む）。演出は CSS / SVG / Vue 標準の
   `<Transition>` / `<TransitionGroup>` で実現する。
8. `npm test` と `npm run build` が通ること。FR-1〜11 の機能退行なし。

## 3. 支給アセット（Fable 取得済み・コミット済み）

| パス | 内容 | ライセンス |
|---|---|---|
| `frontend/src/assets/fonts/outfit/outfit-latin-var.woff2` | Outfit Variable（latin, wght 100–900, 32KB）。幾何学系・Google Sans に近い温度 | OFL（同梱 OFL.txt） |
| `frontend/src/assets/fonts/space-grotesk/space-grotesk-latin-var.woff2` | Space Grotesk Variable（latin, wght 300–700, 22KB）。テック寄り・エッジのある表情 | OFL（同梱 OFL.txt） |

- 用途: 欧文・数字のディスプレイ用（見出し・カウントダウン・時刻・英字マイクロラベル等）。
  `@font-face` + `font-display: swap` で自己ホスト。和文は現行 Noto Sans JP スタック継続
  （和文 Webフォントは容量的に禁止）。
- **どちらか一方を選ぶ**（両方使いは一貫性を壊すので避ける）。「どちらも使わない」も可。不要側の
  ディレクトリは削除してよい。
- 追加素材（画像・別フォント等）が必要になったら `docs/QUESTIONS.md` に起票（用途とライセンス希望を明記）
  → Fable がホスト環境で取得・検証して支給する（Codex サンドボックスは外部 DNS 不通のため）。

## 4. デザイン方向 — Fable の推奨（Sol は上書き可）

**コンセプト案「Quiet Aurora」**: 画面の 98% を無彩色のダークニュートラルに沈め、ブランドのオーロラは
「AI が生きている瞬間」にだけ現す。

- オーロラの出現箇所を最大 3 つに制限する:
  1. ローディングリング（確定仕様そのまま）
  2. チャット空状態の挨拶テキストのグラデーション（Gemini の "Hello, ○○" に相当する一撃）
  3. 入力欄の streaming 中にだけ現れる微かな縁の光
- 排除する: ヘッダー虹ヘアライン / アクティブスレッド虹バー / 入力欄ミント発光 / 虹枠チップ / 3 色アバター。
  置き換えは無彩色（面・ボーダー・白）＋必要なら単色アクセント 1 色まで。
- テキスト用グラデは純黄 #FFEB3B を経由しない補正系が上品（例: #FF8F6D → #FFC46B → #6FE8A8）。
  リングの STEP_THEMES とは別トークンとして定義する（リング側は変更不可）。
- ユーザー吹き出しは白をやめ、Gemini ダークテーマ風のソフトな面色（例: raised より一段明るい無彩面）＋白文字。
- ニュートラル階調はごく僅かに暖色へ寄せると、アプリアイコン（オレンジの歯車・赤い Y・緑の CPS lab）と喧嘩しない。
- 数字・欧文にディスプレイフォント（§3）。カウントダウンの「5」、時刻表の「10:00」、「AI」「2026」が締まる。
- ログイン: ライトエリア左上にブランド行（アプリアイコン＋「本荘キャンパス案内 AI」＋英字マイクロラベル）、
  白面には 4〜6% 不透明度のオーロラの気配（超低彩度・低速ドリフト、reduced-motion で静止）、
  シート内の「ログインはこちら」全幅ボタンはセグメントコントロール等の明快な UI へ。
  PC ではライトエリアの見出しを大きく（〜text-6xl）、余白を「意図した静けさ」に見せる。
- チャット: ヘッダーは無彩に静める。空状態はグラデ挨拶＋整ったサジェストカード（矢印装飾は削る）。
  入力バーは Gemini 風の浮遊ピル（下辺ボーダーで区切らず、余白とフェードマスクで浮かせる）。
  送信ボタンは有効時に白 or 単色（虹をやめる）。出典チップ・サイドバーは無彩の面と余白で整える。

**繰り返す: 以上は参考案。** Design Arena 首位の審美眼で、§0 の利用者の言葉と §2 の制約を守る限り、
これより良い解を自由に採ってよい。

## 5. モーション要求（「何を」= MUST、「どう」= Sol の裁量）

存在すべき演出（実装の仕方・味付けは自由）:

1. ログイン画面の初回入場シーケンス（要素が段階的に現れる。全体 1 秒以内に静定）
2. ログイン成功 → チャットへの画面遷移トランジション（ぶつ切り禁止。router の `<RouterView>` を
   `<Transition>` でラップする等。App.vue の変更は可）
3. チャット空状態の入場（挨拶・チップのスタッガー等）
4. 送信時のユーザー吹き出し出現アニメーション
5. モバイルドロワーの開閉（オーバーレイとパネルの滑らかな連動。現状 200ms 一律から質感を上げる）
6. hover / active / focus のマイクロインタラクション統一 — duration・easing をトークン化して全体に適用
7. 上記すべて `prefers-reduced-motion` で無効化（既存のグローバル抑止ルールを活かす）

推奨: 入力ピルの focus 遷移、スレッド切替時のメッセージ領域クロスフェード、ダイアログ入退場の磨き込み、
送信ボタンの有効化トランジション。

禁止: リング以外の常時ループ演出の多用（ログイン白面の超低彩度ドリフトのような「気配」レベルは可）、
300ms を超えて操作をブロックする遷移、視差スクロール等の重い効果、レイアウトシフトを起こす演出。

## 6. 実装ノート

- ブランチ: `feature/ui-restyle-sol`。コミットはローカルまで（push・PR 作成は不要。マージは Fable が行う）。
- 変更範囲: `frontend/` のみ。`docs/UI_POLISH_V2.md` への設計ノート追記（§7-4）は可。
- トークン集約: 色・radius・shadow・duration・easing は `tailwind.config.js` と `style.css` の CSS 変数に
  集約し、コンポーネント内のマジックナンバー散在を残さない。FR-9 で導入した ink/edge/fill/brand トークンは
  再設計してよい（名前含む）。
- `index.html` の `theme-color` 等メタ調整も対象。favicon はアプリアイコンのまま。
- 動作確認環境（ライブスタックと衝突しないこと）:
  - backend: `AGENT_MODE=mock MOCK_STATUS_DELAY_SECONDS=2.5 uvicorn app.main:app --port 8081`
    （リポジトリ直下で `python -m venv` 等は不要。既存の実行方法は backend/README または compose を参照）
  - frontend: `VITE_API_PROXY_TARGET=http://127.0.0.1:8081 npm run dev -- --port 5174`
  - ※ 5173/8080 は本番 compose が使用中。使わないこと。
- 検証ビューポート: 390×844 / 768×1024 / 1440×900 の 3 幅 × 主要状態（ログイン・空チャット・会話中・
  ストリーミング中・ドロワー/サイドバー・ダイアログ）。
- `npm test` / `npm run build` を完了報告前に必ず実行。

## 7. 検収（評価ループ — FR-9 §5 方式を継承・基準引き上げ）

1. 実装完了後、評価エージェント（実装と別人格）が mock 環境を Playwright で操作・撮影し、下のルーブリックで採点。
2. ルーブリック: FR-9 §4 の 10 観点 × 10 点。ただし観点 1 を「第一印象 — **『AI が作った量産 UI』感の脱却**を
   最重要指標とする」に改める。観点 6（マイクロインタラクション）は §5 の MUST 7 項の充足を前提とする。
3. **合格: 93 点以上**（FR-9 は 91 点合格だったが、その状態を利用者が「いかにも AI が作った UI」と評した。
   よって基準を引き上げる）。
4. 不合格なら減点を「観点・画面・具体箇所・修正提案」形式で Codex / Sol に差し戻し、最大 3 巡。
   合格後、Sol は §4 に対する「Sol 設計ノート」（採用した概念・色/タイポ/モーションのトークン表・
   推奨を破った箇所と意図）を本ドキュメント末尾に追記する。
5. 最終の採点表・スクリーンショット所見・設計ノートを Fable がレビューして merge。

## 8. Sol 設計ノート（実装後に Sol が追記）

### 8.1 採用概念 — Campus Signal

「大学の案内所」と「当日のウェイファインディング」をデザインの核にした。ログインは来場者を迎える生成りの
紙面、チャットは情報を読み込む暖色寄りの黒鉛面とし、ガラスカードと虹色発光に頼る汎用 AI チャットの語彙を
排した。ブランドカラーは単色の Signal Coral を現在地・操作開始・選択状態にだけ置き、Aurora は
LoadingSpinnerV5、空状態の利用者名、streaming 中の入力欄の 3 箇所に限定した。

### 8.2 デザイントークン

| 分類 | トークン | 値 / 用途 |
|---|---|---|
| Neutral | canvas / panel / raised / high | `#0D0F0E` / `#131614` / `#1B1E1C` / `#252925` |
| Light | paper / paper-ink | `#F0EFE9` / `#171916`（ログインのライトエリアと主要 CTA） |
| Text | primary / muted / dim | `#F2F1EC` / `#A9ADA7` / `#777C76`（canvas 比 17.01 / 8.45 / 4.51:1） |
| Accent | signal / signal-soft | `#FF7657` / `#FF9A80`（単色の案内標識） |
| Aurora copy | warm / bridge / mint | `#FF8F70` / `#FFC46B` / `#6FE8A8`（空状態の名前のみ） |
| Typography | display | 同梱 `Space Grotesk Variable`。英字・数字・見出し用。和文は Noto Sans JP 系スタック |
| Radius | sm / md / lg / sheet | `10 / 16 / 24 / 40px` |
| Shadow | hairline / raised / overlay | 内側 1px / 低く広い浮上影 / ドロワー・ダイアログ用の深い影 |
| Motion | fast / base / slow / stagger | `140 / 220 / 480 / 60ms` |
| Ambient | atmosphere | `16s`。ログインの単色低彩度ハローのみ。操作を妨げない |
| Easing | standard / expressive | `cubic-bezier(.2,.8,.2,1)` / `cubic-bezier(.16,1,.3,1)` |

Outfit は採用せず、ディレクトリごと削除した。全フォントは自己ホストで、実行時ネットワークアクセスはない。
`prefers-reduced-motion` では入場、遷移、stagger、hover の移動、ambient drift を停止し、タイプライターは既存ロジックで
フレーズ 1 の静的表示へ切り替わる。

### 8.3 画面への適用

- ログイン: 2 層構造を維持し、ライトエリアを編集的なブランド面、下部を Visitor Desk として再構成した。
  PC は説明とフォームを横並びにして余白へ役割を与え、モバイルは 390px の縦リズムに合わせて直列化した。
- チャット空状態: 中央の発光ロゴを主役にせず、左揃えの挨拶、日程、番号付き質問導線で「案内所」の世界観を作った。
- 会話中: AI 回答は中央ドキュメント、利用者発言は同カラム右端の graphite bubble。出典、サイドバー、ダイアログは
  無彩色の elevation と余白で整理した。
- 入力欄: 下辺の区切り線をなくした floating composer。通常 focus は無彩色、streaming 中だけ Aurora の細い縁を出す。
- モーション: ログイン初回入場、route 遷移、空状態 stagger、利用者吹き出し、ドロワー、統一 hover/focus/active の
  FR-12 §5 MUST 7 項を CSS / Vue Transition だけで実装した。

### 8.4 Fable 推奨から調整した箇所

- ログイン背景の「オーロラの気配」は多色 drift にせず、Signal Coral 1 色・最大 8% のハローにした。
  ログインで Aurora を消費せず、チャットの「AI が動く瞬間」との意味差を明確にするため。
- 空状態のグラデーションは挨拶全文ではなく利用者名だけに限定した。日本語見出しの可読性と色の一撃を両立するため。
- アプリアイコンは大きな発光オーブにせず、角丸の小さな識別標として扱った。既存ロゴの多色を面積で抑え、
  単色アクセント体系と共存させるため。
- Gemini 的な丸い浮遊入力は採用したが、focus 時の mint glow は使わず無彩色の hairline とした。
  Aurora は streaming 中だけ現れるという意味規則を守るため。

`LoadingSpinnerV5.vue` は SVG、テーマ、keyframes、timing、morph、shimmer、`type="transition"` を含め差分なし。

## 9. FR-13 追補仕様（2026-07-14 利用者指示・v0.3）— APU-Navi ブランド化と 5 点ブラッシュアップ

利用者評価:「UI はかなり良くなっています」。その上で以下 5 点。デザインリードは引き続き Sol。
FR-12 の §2 不可侵制約はすべて継続（LoadingSpinnerV5 / タイプライター / 中央カラム / a11y / オフライン堅牢 /
新規依存禁止）。**例外として本追補では backend の LLM プロンプト内文字列の変更のみ許可**（§9-5。構造・API・
SSE・store は引き続き不変）。94 点の現行デザイン言語（Campus Signal）を壊さず増分で磨くこと。

### 9-1. Gemini 風アンビエント背景（雲）

- Gemini アプリの特徴である「もやーっとした雲のような背景」を、**派手すぎず主張しすぎない**水準で再現し
  現行 UI に馴染ませる（利用者の参照スクリーンショットは取得不可だったため、意匠要件で定義する）。
- 要件: 画面の隅〜端に、大きくぼかした低彩度のグラデーション雲を 2〜3 個。チャット画面（ダーク面)が主対象。
  ログイン側への適用は Sol 裁量（既存の紙面の表情と喧嘩させない）。
- 濃度はごく淡く（体感 5〜10% 程度）、ゆっくり漂う（60s 級。`prefers-reduced-motion` で静止）。
- 色は Campus Signal パレットの低彩度域を基本とし、冷色を 1 色補うのも可。ただし「オーロラは AI が生きて
  いる瞬間だけ」の意味規則（§8）を濁さないこと（雲は無意味な常時装飾ではなく「空気」に徹する）。
- 実装は transform/opacity のみのアニメーションでモバイル 60fps を維持。本文コントラスト比を劣化させない。

### 9-2. チャット画面の情報密度削減

- 画面上の文字量が多い。**アイコンの存在感を活かし、文字と文字装飾を減らす**。
- 対象例: ヘッダーの英字マイクロラベル群・サイドバーの eyebrow・空状態の重複ラベル。英字ラベルは
  「効かせる 1〜2 箇所」に絞り、他は削除または アイコン/余白に置換（どれを残すかは Sol 裁量）。
- アプリアイコン（APU-Navi の顔）をヘッダー・空状態・サイドバーで主役に使う。

### 9-3. サジェスト例文の刷新（4 個のまま）

本システムの特徴（学内ナレッジ QA・**経路案内 FR-11**・場所特定・アクセス情報）を体現する 4 例文に差し替える。
内容は以下で確定（文言の微調整のみ Sol 可。すべて現行ナレッジで回答可能なことを Fable 確認済み）:

1. 「無料送迎バスの時刻は？」（アクセス）
2. 「模擬講義は何がある？」（当日イベント）
3. 「食堂から D404 への行き方は？」（**経路案内** — 本システムの看板機能）
4. 「個別進学相談はどこでやってる？」（場所特定）

**2026-07-17 改訂（FR-31・利用者指示「地図で経路案内できる特徴を例文に」。SPEC v0.23）**:
マップカード（FR-26〜30）の 3 モードを体現する構成へ差し替え。全例文が実際にカードを表示する
（マップタップ/route/place）ことを Fable が実 LLM で確認済み。実装は文言スワップのみのため
Fable 直接編集（役割分担の例外・コミットに明記）:

1. 「サイバーフィジカルシステム研究室に行きたい」（**マップタップ** — 現在地を聞かれて地図から選ぶ体験。
   研究室名での経路案内 FR-29 のショーケース）
2. 「総合受付から学部棟Ⅰへの行き方は？」（**経路カード** — 来場者の実起点からの 2 点間経路）
3. 「GI512 はどこ？」（**場所カード** — 部屋番号の場所特定）
4. 「無料送迎バスの時刻は？」（アクセス — マップ以外の回答力も見せる・保護者向け。従来から継続）

### 9-4. 空状態あいさつのランダム化（Gemini 挙動の再現）

- チャット画面（空状態）を開くたびに、あいさつ文言をランダム表示する。文言セット（Sol が微調整・追加可）:
  - 「{name}さん、なんでも聞いてください」
  - 「何から始めますか？」
  - 「{name}さん、さっそく始めましょう」
  - 「今日はどんなことを知りたいですか？」
  - 「ようこそ、{name}さん」
- name 未取得時は名前なしバリアントへフォールバック。直前と同じ文言の連続は避ける。
- グラデーションの当て方（名前 or キーフレーズ）は Sol 裁量（オーロラ規則の範囲内）。
- 長いニックネーム（20 文字）でも孤立行が出ないこと（R2 修正の維持 — 回帰ガード）。

### 9-5. AI の命名「APU-Navi」

- **UI**: 表示名「本荘キャンパス案内 AI」を「APU-Navi」に変更（Gemini アプリのプロダクト名表示に相当）。
  ログインのブランド行・チャットヘッダー・サイドバー・index.html の title/meta・alt/aria-label が対象。
  「本荘キャンパス案内」「AKITA PREFECTURAL UNIVERSITY」等をサブラベルとして残す構成は Sol 裁量。
  表記は「APU-Navi」で固定（全角・カナ化しない）。
- **backend（本追補で唯一許可される backend 変更）**: `backend/app/agent/graph.py` の
  `GENERATE_SYSTEM_PROMPT` 冒頭でペルソナに名前を付与する
  （例: 「あなたは『APU-Navi（エーピーユー・ナビ）』。秋田県立大学 本荘キャンパスの…学生ガイドAIです。」）。
  名乗りは自己紹介を求められた時・挨拶時のみとし、毎回答で名乗らないルールを 1 文添える。
  同ファイル内の analyze/evaluate 用ロールプロンプトの「本荘キャンパス案内AI」も「APU-Navi」へ置換。
  プロンプト以外のコード・構造・API は変更しない。backend の pytest が通ること。
- ドキュメント: SPEC.md（§1・§2・FR-13）は Fable が更新済み。AGENT_HARNESS.md の版数追記は Sol が行ってよい。

### 9-6. 検収（FR-13）

増分ブラッシュアップのため評価エージェントの採点ループは省略し、**Fable が実機検収**する:
3 幅スクリーンショット（390/768/1024/1440 相当）× ログイン・空状態（ランダム挨拶を複数回リロード）・
会話中・ストリーミング、frontend `npm test`/`npm run build`、backend `pytest`、
デプロイ後に実 LLM へ「あなたの名前は？」で APU-Navi の名乗りを確認。


## 10. FR-14 追補仕様（2026-07-14 利用者指示・v0.4）— Gemini 実スクショ準拠のアンビエント背景

### 10-0. 発端（利用者指示 2026-07-14）

> UIについて，GeminiアプリのUI（referenceUIフォルダのスクショ）のように，もやーっと雲のような背景を
> 再現してください．現状，ログイン画面は白ベース，チャット画面は黒ベースになっています．
> 各パターンのGeminiアプリのUIをスクショしてしたので，ログイン画面とチャット画面それぞれで参考にしてください．

### 10-1. 経緯と位置づけ

- FR-13 §9-1 のアンビエント雲は**参照スクショが入手できなかったため意匠要件のみで代替実装**した
  （Campus Signal 低彩度 3 色 clay/slate/moss を四隅に配置）。
- 今回、利用者が Gemini アプリの実スクリーンショット 4 枚を `referenceUI/` に提供した。
  **本 §10 ではこのスクショを意匠の正とし、§9-1 の雲実装を置き換える**。
  - `referenceUI_white_PC.png` / `referenceUI_white_phone.png` → **ログイン画面**（白ベース）の参照
  - `referenceUI_black_PC.png` / `referenceUI_black_phone.png` → **チャット画面**（黒ベース）の参照
  - ※ `referenceUI/` は利用者指示（2026-07-14）により **.gitignore のローカル保持のみ**（コミットしない。
    chat.mov と同扱い）。リポジトリに画像はないが、合わせ込みに必要な実測値は §10-2 に転記済みで自足する。
- デザインリードは引き続き Sol。FR-12 §2 不可侵制約はすべて継続。変更対象は
  `frontend/src/style.css`・`ChatView.vue`・`LoginView.vue`（+必要なら tailwind.config.js のトークン追加）のみ。
  backend・stores・API・LoadingSpinnerV5・タイプライターは触らない。

### 10-2. 参照スクショの実測値（Fable がピクセルサンプリング済み・これを合わせ込み目標とする）

Gemini の「もやーっとした雲」の正体は、**無地ベースの上に置かれた超ソフトな単色ラジアルグロー**
（白版=パステルブルー、黒版=深いインディゴ）。多色ではない。輪郭は一切見えず、四隅は必ずベース色に戻る。

| 参照 | ベース（四隅・上端） | グロー最濃部（実測） | 分布 |
|---|---|---|---|
| white_PC | `#fbfbfc`〜`#fdfcfc`（微冷白） | `#c9e2fb`（y≈0.58 中央） | 中央の横長楕円。x0.32〜0.68 が濃く、x0.08/0.92 でほぼ消失。縦は y0.40〜0.70 が濃く、y0.15/0.96 でほぼ白 |
| white_phone | `#fdfcfc` | `#d5e7fb` 前後（画面最下部） | 上 65% は完全な白。y0.70 から下へ徐々に青くなり最下端が最濃。左右差ほぼなし（横一様の霧） |
| black_PC | `#0f0f0f`〜`#101010`（無彩黒） | `#151b39`（y≈0.58 中央） | white_PC と同じ中央楕円分布。ベースに対しほぼ B チャンネルだけが持ち上がる |
| black_phone | `#000000`（純黒） | `#1a224e`（画面最下端） | 上 60% は純黒。y0.65 から下へ立ち上がり最下端が最濃（`#0a0e21`@y0.80 → `#0e132e`@y0.84 → `#1a224e`@y0.975）。横方向ほぼ一様 |

補足観察:
- PC 2 枚には極めて淡い同心円リップルの気配があるが、なくても世界観は成立する（→ 10-5 の MAY）。
- グローの縁は完全に溶けており、グラデーションのバンディングは見えない。
- 入力ピル・テキストはグローの上に浮いており、グローが文字コントラストを侵食していない。

### 10-3. チャット画面（ダーク面）要件

1. **§9-1 の clay/slate/moss 3 雲は撤去**し、Gemini 黒版準拠のインディゴグローに置き換える。
2. **空状態（メッセージ 0 件）**:
   - PC（lg 以上）: メインカラム中央（挨拶見出し〜コンポーザー帯）を中心とする大きな横長楕円グロー 1 つ。
     レンダリング結果の最濃部が **`#151b39` ±10/ch** になるよう合わせ込む。カラム四隅・上端はベース
     （canvas `#0d0f0e`）へ完全に溶けて戻ること。
   - モバイル（lg 未満）: black_phone 準拠。上部はベースのまま、**画面下端から立ち上がる霧**
     （最濃部 `#1a224e` 級を下端に、y≈0.6 より上では消失）。コンポーザーの背後で最も濃くなる。
3. **会話中（メッセージ 1 件以上）**: グローは消さず「鎮める」。下部（コンポーザー帯）に寄せる・
   強度を体感 50〜60% に落とす等で、本文の可読性を最優先する。切り替えは既存のリアクティブ状態
   （`chat.messages.length`）で `.chat-shell` にモディファイアクラスを付け、CSS transition
  （opacity/transform のみ）で滑らかに。
4. **composer-dock との整合（既知の統合ポイント）**: 現行 `.composer-dock` は canvas 不透明色への
   フェードで下端を塗り潰すため、そのままでは「下端が最も濃い青」と矛盾する。対応例:
   ドックの最終ストップを `color-mix(in srgb, var(--color-canvas) 60%, #1a224e 40%)` 級の
   「グローを含んだ闇色」にする等。**長文メッセージがコンポーザー下を通過する際の可読性を落とさない**こと。
5. ベースの elevation トークン（canvas/panel/raised/high）は**変更しない**。ヘッダーの
   `bg-ink-base/[0.88] backdrop-blur`・不透明サイドバーもそのまま（Gemini 参照でもグローは中央域で消える
   ため、サイドバー背後まで届かなくてよい）。

### 10-4. ログイン画面（ライト面）要件

1. **ヒーロー領域のベース色を微冷白 `#fbfcfe` 級に変更**（現行の暖色紙 `#f0efe9` のままでは青い霧が
   濁るため。変更はログイン画面スコープに限定し、`--color-paper` トークン自体や黒シート上の
   `bg-ink-paper` ボタンへの波及は Sol 裁量で最小に）。
2. **現行の Signal Coral ハロー（`.login-hero::before`）はブルーの霧に置き換える**。コーラルは
   マイクロアクセント（ドット・カーソル・selection・ボタン）にのみ残す。
3. PC（lg 以上）: white_PC 準拠。見出し帯を中心とする横長楕円の霧。レンダリング最濃部
   **`#c9e2fb`〜`#d5e8fb` ±10/ch**。ヒーロー四隅・上端は `#fbfcfe` 級に戻る。
4. モバイル: white_phone 準拠。ヒーロー上部は白のまま、**ヒーロー下端（黒シートとの境界直上）に
   霧が溜まる**。最濃部 `#d5e7fb` 級、ヒーロー高さの上位 55% では消失。
5. 既存のエディトリアル要素（罫線グリッド・ノイズ・左縦ライン・タイプライター）は維持してよいが、
   霧が主役として読めるようグリッド濃度の減衰（例: 4.2% → 2% 前後）は Sol 裁量。
   黒シート `#111310` の色相を霧と調和させる微調整（無彩寄せ）も Sol 裁量（構図・レイアウトは不変）。

### 10-5. 共通要件

- **意味規則の継続**: 霧/グローは常時の「空気」。Aurora（多色）は AI 稼働中のみ、という FR-12 §8 の
  規則は不変。ブルーを新たなアクセント色としてボタン・リンク等に展開しない（空気に徹する）。
- **モーション**: ほぼ静止。60〜90s 級の呼吸（opacity ±20% 相対・scale ≤4%・translate ≤3%）を
  transform/opacity のみで。`prefers-reduced-motion` で完全静止（既存の `.ambient-cloud` 静止規則を継承）。
- **MAY（Sol 裁量）**: PC のみ極淡い同心円リップル（体感 α≤3%）。1 段でもバンディングが見えるなら不採用。
- **実装制約**: CSS グラデーションのみ（canvas/WebGL/画像/新規依存の禁止）。モバイル 60fps 維持。
  バンディング対策として多層グラデーション or 大径ぼかしを用い、1440px スクショで縞が見えないこと。
- **a11y**: 本文テキストのコントラストは現行水準を維持（ダーク面 白/87 vs グロー最濃部でも 13:1 以上が
  出る計算。ライト面 `#171916` vs `#c9e2fb` も 12:1 以上）。フォーカスリング視認性も現行維持。

### 10-6. 検収（FR-14・Fable 実機）

1. Playwright で 390/768/1440 × ログイン・チャット空状態・会話中・ストリーミングのスクショ取得。
2. **ピクセル検証**: 参照と同アナロジー点（グロー最濃部・四隅・上端）をサンプリングし、
   10-2 の目標値と **±10/ch 以内**（四隅はベース ±4/ch 以内）で一致すること。Fable が §10-2 と同じ
   スクリプトで機械照合する。
3. frontend `npm test` / `npm run build` 通過。LoadingSpinnerV5.vue は差分ゼロ。
4. reduced-motion で霧が静止すること、会話中→空状態の切り替えでグローが滑らかに遷移することを目視確認。

### 10-7. 検収記録（2026-07-14 Fable 実機・合格）

- 実装: Codex / GPT-5.6Sol（task-mrk65glg-zhtb2d, xhigh）。変更は style.css / ChatView.vue のみ
  （LoadingSpinnerV5.vue 差分ゼロ、backend 変更なし）。ログイン側は CSS のみで実現（マークアップ不変）。
- 静的検証: frontend `npm test` 21/21・`npm run build` 成功（Fable 再実行で確認）。
- 実機検証: mock スタック（8081/5174 分離環境）+ Playwright、390/768/1440 × ログイン・空状態・会話中・
  ストリーミング。呼吸アニメは中間位相で一時停止して測定（決定性確保。振幅は目標許容 ±10/ch に内包）。
- ピクセル機械照合（参照実測値 §10-2 比）:
  - チャット空状態 1440: グロー最濃部 `#141a38`（目標 `#151b39`、Δ1）。四隅・上端・メインカラム左は
    canvas `#0d0f0e` に Δ0〜2 で回帰。縦プロファイルの段差 ≤4/2%vh でバンディングなし。
  - 会話中 1440: 同一測定点でグロー成分が空状態比 56%（仕様 50〜60%）。本文帯 (0.6,0.3) は `#0c0e0d`≒素の
    canvas でコントラスト劣化なし。
  - チャット空状態 390: 下端立ち上がりは滑らか（段差 ≤4）。シャドウ非干渉の下端ガター `#161e47`
    （目標 `#1a224e`、Δ7）。
  - ログイン 1440: 霧最濃部 `#cae4fc`（目標帯 `#c9e2fb`〜`#d5e8fb` 内）。四隅 `#fbfcfe` Δ0。
  - ログイン 390/768: 上部 31%vh まで完全な `#fbfcfe`、以降 `#ddeaf7`〜`#e1edfb` へ単調・滑らかに深化
    （目標 `#d5e7fb` 級に Δ≤8）。
- **既知の逸脱（許容と裁定）**: モバイル/タブレットの画面最下端はコンポーザーピルの既存ドロップシャドウ
  （FR-12 `--shadow-raised`）が глоу に重なり、参照最下端値より B が 15〜19 暗い（768 はピルが全幅のため
  シャドウ非干渉点が存在しない）。シャドウ外の同アナロジー点は許容内であり、視覚上は参照と同等の
  「下端から立ち上がる深紺」を再現できているため許容。ヒーロー下端の暗化（ログイン）も既存シート上向き
  シャドウ由来で霧の欠陥ではない（左右端の霧なし領域と同値であることを確認済み）。
- Sol の裁量記録: 同心円リップルは不採用（バンディング回避）。ログインのグリッド/ノイズは 0.6%/0.4% へ
  減衰。黒シート色は既存値維持。composer-dock はモバイルのみ半透明化（PC は従来の不透明フェード維持）、
  透け方は「ささやき」水準で可読性劣化なしを実機確認。


## 11. FR-15 追補仕様（2026-07-14 利用者指示・v0.5）— 空状態の整理とサジェストチップの操作性

### 11-0. 発端（利用者指示 2026-07-14）

> チャット画面上の初めで提示される「オープンキャンパス2026まで〇日」はなくてOKです．
> チャット画面上部の「APU-Navi」表示部分のアイコンは削除してOK（サイドバー上のアイコンは今のまま
> 削除せずでOK）．「APU-Navi」の下の「本荘キャンパス案内」の文字列も削除してください．
> 提示している例文が，ボタンとして押せる雰囲気がないです．回転矢印を使って，押せば入力欄に
> コピペされる雰囲気を出してください．あともう少し小さくしてください（背景のブラーが隠れてて
> もったいない）．

- FR-12 §2 の不可侵制約はすべて継続（LoadingSpinnerV5 差分ゼロ・中央カラム・a11y・新規依存禁止）。
- **frontend のみの変更**（backend 変更なし）。デザインリードは引き続き Codex / GPT-5.6Sol。
- 対象ファイルの想定: `frontend/src/views/ChatView.vue`・`frontend/src/style.css`（これ以外に波及する
  場合は完了報告に理由を記す）。

### 11-1. カウントダウンチップの削除（MUST）

- 空状態あいさつ下の「オープンキャンパス2026まで あとN日」チップを撤去する。
- `ChatView.vue`: countdown の `<p>`（`chat-empty__support countdown-chip`）、`countdownLabel`
  computed、`OPEN_CAMPUS_DATE` 定数を削除。
- `style.css`: `.countdown-chip` と、専用化していた `.chat-empty__support` の残骸（エンター演出
  グループとディレイ定義）を削除。空状態エンター演出の stagger（identity→heading→support→actions）は
  1 段詰めてよい（リズム維持の采配は Sol 裁量）。

### 11-2. チャットヘッダーの整理（MUST）

- チャットヘッダーの app-icon `<img src="/app-icon.png">` を**モバイル（h-10）・デスクトップ（h-8）とも削除**。
  「APU-Navi」の文字ロゴ（h1）は維持。
- モバイルヘッダーの「本荘キャンパス案内」サブラベル `<p>` を削除。
- **変更しないもの（スコープ外・利用者明示含む）**: サイドバー（ThreadSidebar.vue）のアイコンと
  ラベル一式、空状態中央の h-14 アプリアイコン、ログイン画面、デスクトップヘッダー右端の
  「Honjo / OC 2026」マイクロラベル。
- アイコン・サブラベル亡き後の高さ・縦位置・左端揃えの整えは Sol 裁量（min-h-[68px] は維持してよい）。

### 11-3. サジェストチップの刷新 — 「押せる」見た目と小型化（MUST）

課題（利用者評）: ①ボタンとして押せる雰囲気がない ②大きすぎて背景アンビエント（FR-14 のグロー）を
隠している。

- **挿入アフォーダンス**: 各チップの**右端に「回転矢印」**（曲がりのある矢印グリフ。Enter 記号 ↵ /
  corner-arrow 系の自前 SVG パス）を置き、「押すと例文が入力欄へコピーされる」ことを示す。
  送信（紙飛行機・直線→）や再読込（円形 ⟳）と誤読される形は不可。ホバー/フォーカスで矢印が
  応答する（色・微移動など。詳細は Sol 裁量）。
- **小型化**: 現行（min-h-16・フル幅 2 カラムのカード）から明確に縮小し、背景グローの見える専有面積に
  する。**幅は内容フィット**（w-fit のピル/チップを flex flex-wrap で並べる等。フル幅グリッドで面を
  塗らない）。視覚高さの目安 40〜48px。**実効タップターゲットは 44px 以上を維持**。
- 先頭のカテゴリアイコン 4 種は小型化優先で撤去してよい（存廃は Sol 裁量）。
- 4 例文の文言（§9-3 で確定）・クリックで入力欄へ挿入＋フォーカス（`applySuggestion`）・button 要素で
  あること（キーボード操作・focus-visible）は現行維持。
- アニメーションは transform/opacity のみ（モバイル 60fps）・reduced-motion 配慮は既存方針踏襲。

### 11-4. 検収（FR-15）

Fable 実機検収:

1. 390/768/1440 × 空状態・会話中のスクリーンショット目視（カウントダウン消滅・ヘッダー整理・
   チップの見た目とアフォーダンス）。
2. チップをクリックすると文言が入力欄に入りフォーカスされること（送信はされないこと）。
3. 空状態 1440/390 で、チップ群が背景グロー最濃部を覆い隠していないこと。
4. frontend `npm test` / `npm run build` 通過。LoadingSpinnerV5.vue は差分ゼロ。backend 差分ゼロ。


## 12. FR-16 追補仕様（2026-07-14 利用者指示・v0.6）— 待機中アンビエントと composer フォーカス枠

### 12-0. 発端（利用者指示 2026-07-14）

> 回答を待っている間，背景のブラーにアニメーションを付与してください．
> 入力フォーム欄にカーソルを置いた時に出てくる枠について：もう少し補足して，アイコンの色を踏襲して
> 色が滑らかにゆっくり切り替わるようなアニメーションをつけてください．

- Fable の解釈（裁定）: 「背景のブラー」= FR-14 のアンビエントグロー（`.ambient-glow`）。
  「もう少し補足して」は同音異義の IME 変換ゆれと判断し**「もう少し細くして」**（現行 outline 2px の細線化）
  と読む。誤読だった場合に影響が局所で済むよう、枠の太さは 1 箇所のトークンで持つこと。
- FR-12 §2 の不可侵制約はすべて継続。**frontend のみの変更**（backend 変更なし）。
  対象ファイル想定: `frontend/src/views/ChatView.vue`・`frontend/src/style.css`（波及時は完了報告に理由）。
- デザインリードは引き続き Codex / GPT-5.6Sol。
- オーロラ意味規則（§8「AI が生きている瞬間だけ」）の改訂: 本追補により
  **「待機中の背景の思考」「入力フォーカスの枠」**をオーロラ/アイコン色の出現箇所として正式に追加する
  （対話が生きている瞬間の拡張と位置づける）。

### 12-1. 回答待機中のアンビエント・アニメーション（MUST）

- **いつ**: 送信から回答完了まで（`chat.isSending` が true の間。実装は `.chat-shell` への状態クラス
  付与を想定 — `composer-shell--streaming` と同じ配線で `chat-shell--thinking` 等）。
- **なに**: 背景のアンビエントグローが「考えている」と分かる動きをする。常時の 84s 呼吸
  （`ambient_glow_breathe`）より明確に活発（数秒周期の脈動・ゆらぎ等）、ただし本文より目立たない
  「空気の高揚」に留める。具体の振り付け（脈動 / ドリフト / 濃度の揺れ）は Sol 裁量。
- 制約:
  - transform / opacity のみ（モバイル 60fps。filter・レイアウト誘発プロパティ不可）。
  - 会話中ベース（`--active` の減光 0.55）を土台に、瞬間的に空状態相当まで明るくなるのは可。
    ただし**本文帯のコントラストを劣化させない**（本文背後は素の canvas 近傍を維持）。
  - **開始・終了ともスナップ禁止**（クラス着脱時に輝度が跳ばず、300ms 以上のイージングで馴染むこと）。
    実装注意: 実行中アニメの `animation-duration` 変更は位相ジャンプするため不可。既存 `::before` の
    呼吸と合成する場合は親要素（`.ambient-glow` or `.ambient-clouds`）側に別レイヤーで載せる等、
    機構は Sol 裁量（FR-14 のピクセル恒等性は静止状態でのみ要求 — 待機中は逸脱してよい）。
  - `prefers-reduced-motion` では追加アニメーションなし（既存の全停止ルールに従う）。
  - 待機終了後は FR-14 の通常状態（呼吸のみ）へ完全に戻る。

### 12-2. composer フォーカス枠 — 細線化＋アイコン色スロー・サイクル（MUST）

- 対象: textarea フォーカス時に composer ピルへ出る枠
  （現行 `.composer-shell:has(textarea:focus-visible)` の `outline: 2px solid var(--color-signal-soft)`）。
  **他要素のグローバル focus-visible リング（style.css :where(...) 120 行付近）は変更しない。**
- **細線化**: 2px → **1px 級**（1〜1.5px は Sol 裁量。outline-offset ≥3px を維持し視認性を確保）。
  太さは 1 トークン（CSS 変数）で持ち、後から一撃で調整できるようにする。
- **色サイクル**: アプリアイコン由来の既存オーロラ・トークン（`--aurora-copy` の
  #ff8f70 → #ffc46b → #6fe8a8 系）を**踏襲**した色が、**滑らかに・ゆっくり**（1 周 6〜12 秒、
  ease 系、継ぎ目のないループ）で切り替わる。純度の高い原色化・虹色化はしない（Campus Signal の
  抑制を維持）。停止点・経由色の微調整は Sol 裁量。
- a11y:
  - サイクルのどの瞬間も枠が背景に対し **3:1 以上**のコントラストを維持し、消えないこと。
  - `prefers-reduced-motion` ではサイクル停止し、単色固定（signal-soft か オーロラ 1 色）。
    枠そのもの（フォーカス表示）は必ず出る。
- streaming（`composer-shell--streaming` のオーロラ縁）との共存: 両状態が同時に成立した際に
  視覚が喧嘩しないよう調停する（streaming 優先で outline を沈める / 同期させる等、Sol 裁量。
  完了報告に判断を記す）。

### 12-3. 検収（FR-16）

Fable 実機検収（mock 8081/5174、390/1440）:

1. 待機中: 送信→回答完了までの間、グロー実測値が数秒スケールで振動すること（同一測定点の連続
   サンプリングで確認）。アイドル時は追加アニメがないこと（84s 呼吸のみ）。本文帯の背景が
   canvas 近傍を維持すること。開始・終了でスナップがないこと（連続キャプチャ目視）。
2. フォーカス枠: outline 太さが 1px 級であること。computed outline-color を数秒おきに 3 回以上
   サンプリングし、オーロラ系 3 色域を滑らかに遷移していること。reduced-motion エミュレーションで
   単色固定・枠残存。
3. streaming 中にフォーカスを維持しても縁とoutlineが破綻しないこと（目視）。
4. frontend `npm test` / `npm run build` 通過。LoadingSpinnerV5.vue 差分ゼロ。backend 差分ゼロ。

## 13. FR-17 追補仕様（2026-07-14 利用者指示・v0.7）— FR-16 モーションの知覚強度引き上げ

### 13-0. 発端（利用者指示 2026-07-14）

> 入力欄フォーカス時の枠のアニメーションについて：ゆっくりすぎて気づかないので，もう少し動かしてOKです．
> 回答を待っている間のブラーのアニメーションについて：ブラーのアニメーションも気づかないです．
> 雲が動いているような動きをアニメーションで再現できると望ましいです．

- 位置づけ: FR-16（§12）の 2 演出は機構・検収とも合格したが、**知覚強度が不足**という利用者フィードバック。
  機構（常設レイヤーの opacity 入切り・トークン化した枠太さ・streaming 調停・reduced-motion 方針）は
  そのまま維持し、**振り付けだけを「気づける」強度へ引き上げる**。
- FR-12 §2 の不可侵制約はすべて継続。**frontend のみの変更**
  （対象想定: `frontend/src/views/ChatView.vue`・`frontend/src/style.css`。波及時は完了報告に理由）。
- デザインリードは引き続き Codex / GPT-5.6Sol。

### 13-1. 待機中アンビエント — 「雲が動いている」ドリフトへ再設計（MUST）

- ゴール: 回答待機中、**視線を向ければ 2〜3 秒で「動いている」と分かる**こと。
  現行 §12-1 の振り付け（6.8s その場パルス、レイヤー 0.28、translate ±0.7%）は知覚不能と判定されたので、
  **保持せず作り直してよい**（`ambient_glow_thinking` キーフレームの置き換え可）。
- 要求:
  - **移動が主役**: その場の明滅（opacity パルス）ではなく、雲塊が**横方向主体に流れる**動き。
    複数の雲レイヤー（2〜3 枚）を異なる速度・位相で流して視差を出すことを推奨(Sol 裁量)。
    雲塊用の追加 DOM（`.ambient-thinking-glow` 内への span 追加等）は可。
  - **知覚下限（定量 MUST）**: 少なくとも 1 レイヤーの水平移動が、**1440px 幅時のある 3 秒窓で 24px
    （約 1.7%）以上**。イージングの折り返し付近が遅いのは構わない（「存在すればよい」判定）。
  - 上限は「本文より目立たない空気の高揚」を守る範囲で Sol 裁量。レイヤー不透明度は現行 0.28 から
    **最大 0.55** まで引き上げ可。
  - ループの継ぎ目が見えないこと（端点一致 or alternate）。
- 機構の維持（MUST — FR-16 R2 の教訓をそのまま適用）:
  - 入切りは**常設レイヤーの外側 opacity transition（≥300ms）**で行い、内側アニメは非表示時
    `animation-play-state: paused`。クラス着脱でスナップしない（実測最大ステップ ≤0.05 目安）。
  - transform / opacity のみ（filter・レイアウト誘発プロパティ不可）。ブラー感は静的グラデーションで作る。
  - 本文帯のコントラストを劣化させない（本文背後は素の canvas 近傍を維持。雲は下部グロー域に留める）。
  - `prefers-reduced-motion` では追加アニメなし（レイヤー opacity 0 を維持）。
  - 待機終了後は FR-14 の通常状態（84s 呼吸のみ）へ完全復帰。

### 13-2. composer フォーカス枠 — 色サイクルの高速化（MUST）

- 現行: `composer_focus_aurora` 1 周 9s。利用者は変化に気づけない。
- 要求: **1 周 2.5〜4.5s**（Sol 裁量）へ短縮し、**1 秒眺めれば色が変わっていると分かる**こと。
  - **定量 MUST**: 1 秒間隔で computed outline-color をサンプリングしたとき、隣接サンプル間の
    RGB 差（いずれかのチャネル）が **30 以上**になる区間がサイクル中に存在すること。
  - 色域は引き続き `--aurora-copy` 系（#ff8f70 → #ffc46b → #6fe8a8）を巡回。虹色化・原色化はしない。
    経由色の追加・停止点調整は Sol 裁量。
  - 枠の太さ・位置は動かさない（outline-offset や width のアニメは不可。色の変化のみで強化する）。
    色と同期した控えめな box-shadow グローの追加は Sol 裁量で可（paint 系のみ・派手にしない）。
- 現状維持（MUST）: 1px 太さ（トークン `--composer-focus-ring-width`）・outline-offset ≥3px・
  グローバル focus-visible リング不変・streaming 時は枠を沈める調停・reduced-motion では
  サイクル停止＋単色固定（枠は必ず出る）・サイクルのどの瞬間も背景に対しコントラスト 3:1 以上。

### 13-3. 検収（FR-17）

Fable 実機検収(mock 8081/5174、390/1440):

1. 待機中: ≤500ms 間隔の連続サンプリングで、雲レイヤーの translateX 実測に 3 秒窓 24px 以上（1440 時）の
   区間があること。2 フレーム比較スクショで「流れている」ことが目視で分かること。
   開始・終了スナップなし（30ms ポーリングで実測最大ステップ ≤0.05）。アイドル時は追加アニメが
   完全停止（paused・レイヤー 0）していること。本文帯コントラスト非劣化。
2. フォーカス枠: 1s 間隔サンプリングで隣接 RGB 差 ≥30 の区間を確認。周期が 2.5〜4.5s であること。
   3 色域を巡回すること。
3. reduced-motion エミュレーション: 雲レイヤー opacity 0・枠は単色固定で残存。
4. streaming 併存で破綻なし（目視）。
5. frontend `npm test` / `npm run build` 通過。LoadingSpinnerV5.vue 差分ゼロ。backend 差分ゼロ。

### 13-4. 検収記録（2026-07-14 Fable 実機・合格）

実装は Sol: 近景/中景/遠景の 3 雲レイヤー（11s/16s/23s、alternate、負 delay で位相分散、
linear で終端の減速なし）＋ composer 枠 3.6s linear（トークン `--motion-composer-focus`）。

1. ドリフト（1440）: 近景 translateX 実測 -27px → +47.1px の単調移動、**3 秒窓で 43.0px**（基準 24px）。
   2 フレーム比較スクショで雲塊の移動が目視明確。レイヤー opacity 0.52（上限 0.55 内）。
2. スナップなし: 30ms ポーリング 421 サンプル（入→待機→出の全区間）で**最大ステップ 0.043**（基準 ≤0.05）。
   終了後はレイヤー 0・全雲 paused（アイドル完全復帰）。
3. フォーカス枠: 1px / offset 3px / 3.6s linear。1s 間隔サンプルの隣接 RGB 差 **40/88/120/37/90 — 全区間 ≥30**
   （基準は「存在」）。色は #ff8f70→#ffc46b→#6fe8a8 域を巡回。
4. streaming 併存: outline rgb(255,143,112) 固定・animation none（調停維持）。
5. reduced-motion: 雲レイヤー opacity 0・アニメ none、枠は単色コーラル 1px で残存。
6. 390: 雲は下部域に留まり本文可読性維持（ドリフト 10.2px/2s — 画面比で 1440 と同等）。
7. npm test 21/21・build 成功。LoadingSpinnerV5.vue / backend 差分ゼロ。box-shadow グローは Sol 判断で不採用
   （常時発光感の回避）。
