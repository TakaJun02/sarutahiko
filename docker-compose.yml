services:
  vllm:
    image: vllm/vllm-openai:latest
    gpus: all
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ${HOME}/.cache/huggingface:/root/.cache/huggingface
    environment:
      PYTORCH_CUDA_ALLOC_CONF: expandable_segments:True
    command: >-
      --model google/gemma-4-12B-it-qat-w4a16-ct
      --max-model-len 16384
      --max-num-seqs 8
      --gpu-memory-utilization 0.92
      --limit-mm-per-prompt '{"image": 0}'

  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "127.0.0.1:6333:6333"
    volumes:
      - ./qdrant_data:/qdrant/storage

  backend:
    build:
      context: ./backend
    depends_on:
      - qdrant
      - vllm
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      AGENT_MODE: ${AGENT_MODE:-real}
      DATABASE_PATH: /app/data/campus-guide.sqlite3
      VLLM_BASE_URL: http://vllm:8000/v1
      LLM_MODEL: ${LLM_MODEL:-google/gemma-4-12B-it-qat-w4a16-ct}
      LLM_CONTEXT_WINDOW: ${LLM_CONTEXT_WINDOW:-16384}
      LLM_ANSWER_MAX_TOKENS: ${LLM_ANSWER_MAX_TOKENS:-1024}
      QDRANT_URL: http://qdrant:6333
      QDRANT_COLLECTION: ${QDRANT_COLLECTION:-campus_knowledge}
      EMBEDDING_BASE_URL: http://${Second_GPUsever}:8001/v1
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-Qwen/Qwen3-Embedding-8B}
      KNOWLEDGE_DIR: /app/knowledge
      AGENT_TRACE: ${AGENT_TRACE:-1}
      TAVILY_API_KEY: ${TAVILY_API_KEY:-}
      tavily_API: ${tavily_API:-}
      CORS_ALLOW_ORIGINS: http://localhost:5173,http://127.0.0.1:5173
    volumes:
      - ./backend/data:/app/data
      - ./knowledge:/app/knowledge:ro
      - ${HOME}/.cache/huggingface:/root/.cache/huggingface

  frontend:
    build:
      context: ./frontend
    depends_on:
      - backend
    ports:
      - "127.0.0.1:5173:5173"
